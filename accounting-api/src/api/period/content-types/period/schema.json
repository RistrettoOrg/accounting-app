{
  "kind": "collectionType",
  "collectionName": "periods",
  "info": {
    "singularName": "period",
    "pluralName": "periods",
    "displayName": "Period",
    "description": ""
  },
  "options": {
    "draftAndPublish": true
  },
  "attributes": {
    "start_date": {
      "type": "date",
      "required": true
    },
    "end_date": {
      "type": "date",
      "required": true
    },
    "status_period": {
      "type": "enumeration",
      "enum": [
        "open",
        "closed"
      ]
    },
    "name": {
      "type": "string",
      "required": true,
      "unique": true
    }
  }
}
